export enum Layout {
  Main = 'main-layout',
  SideNav = 'side-nav-layout',
}

export enum Method {
  Get = 'GET',
  Post = 'POST',
  Put = 'PUT',
  Delete = 'DELETE',
}

export enum YN {
  Y = 'Y',
  N = 'N',
}

export enum ServiceType {
  Public = 'PUBLIC',
  Private = 'PRIVATE',
}

export enum MenuType {
  Iframe = 'IFRAME',
  Group = 'GROUP',
  Page = 'PAGE',
  NewTab = 'NEW_TAB',
  Iris = 'IRIS',
  Content = 'CONTENT',
  Web = 'WEB',
}

export enum FormMode {
  Create = 'create',
  Edit = 'edit',
}
